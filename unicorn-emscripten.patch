diff --git a/CMakeLists.txt b/CMakeLists.txt
index 033db096..afae3b8d 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -279,6 +279,12 @@ else()
                 set(UNICORN_TARGET_ARCH "loongarch64")
                 break()
             endif()
+            # Check for Emscripten compiler
+            string(FIND "${CMAKE_C_COMPILER}" "emcc" UC_RET)
+            if(${UC_RET} GREATER_EQUAL "0")
+                set(UNICORN_TARGET_ARCH "i386")
+                break()
+            endif()
             message(FATAL_ERROR "Unknown host compiler: ${CMAKE_C_COMPILER}.")
         endwhile(TRUE)
     endif()
diff --git a/glib_compat/glib_compat.c b/glib_compat/glib_compat.c
index 2bb71828..88f68608 100644
--- a/glib_compat/glib_compat.c
+++ b/glib_compat/glib_compat.c
@@ -200,7 +200,7 @@ static GList *g_list_sort_merge(GList *l1,
 
     while (l1 && l2)
     {
-        cmp = ((GCompareDataFunc) compare_func) (l1->data, l2->data, user_data);
+        cmp = ((GCompareFunc) compare_func) (l1->data, l2->data);
 
         if (cmp <= 0)
         {
@@ -337,7 +337,7 @@ static GSList *g_slist_sort_merge (GSList *l1,
 
     while (l1 && l2)
     {
-        cmp = ((GCompareDataFunc) compare_func) (l1->data, l2->data, user_data);
+        cmp = ((GCompareFunc) compare_func) (l1->data, l2->data);
 
         if (cmp <= 0)
         {
diff --git a/include/unicorn/unicorn.h b/include/unicorn/unicorn.h
index b4ffc6bc..92c1ce6f 100644
--- a/include/unicorn/unicorn.h
+++ b/include/unicorn/unicorn.h
@@ -1086,7 +1086,7 @@ uc_err uc_emu_stop(uc_engine *uc);
 */
 UNICORN_EXPORT
 uc_err uc_hook_add(uc_engine *uc, uc_hook *hh, int type, void *callback,
-                   void *user_data, uint64_t begin, uint64_t end, ...);
+                   void *user_data, uint64_t begin, uint64_t end, uint32_t extra);
 
 /*
  Unregister (remove) a hook callback.
diff --git a/qemu/accel/tcg/cpu-exec.c b/qemu/accel/tcg/cpu-exec.c
index 77168f35..fd87a2d7 100644
--- a/qemu/accel/tcg/cpu-exec.c
+++ b/qemu/accel/tcg/cpu-exec.c
@@ -573,7 +573,7 @@ int cpu_exec(struct uc_struct *uc, CPUState *cpu)
 
     // Unicorn: We would like to support nested uc_emu_start calls.
     /* prepare setjmp context for exception handling */
-    // if (sigsetjmp(cpu->jmp_env, 0) != 0) {
+    // if (setjmp(cpu->jmp_env) != 0) {
     if (sigsetjmp(uc->jmp_bufs[uc->nested_level - 1], 0) != 0) {
 #if defined(__clang__) || !QEMU_GNUC_PREREQ(4, 6)
         /* Some compilers wrongly smash all local variables after
diff --git a/qemu/configure b/qemu/configure
index cfd344f7..28227f7c 100755
--- a/qemu/configure
+++ b/qemu/configure
@@ -242,6 +242,7 @@ membarrier=""
 debug="no"
 sanitizers="no"
 strip_opt="yes"
+tcg_interpreter="yes"
 bigendian="no"
 mingw32="no"
 EXESUF=""
diff --git a/qemu/include/exec/helper-gen.h b/qemu/include/exec/helper-gen.h
index c77990f5..271359a5 100644
--- a/qemu/include/exec/helper-gen.h
+++ b/qemu/include/exec/helper-gen.h
@@ -6,54 +6,66 @@
 
 #include "exec/helper-head.h"
 
-#define DEF_HELPER_FLAGS_0(name, flags, ret)                            \
+
+         #define DEF_HELPER_FLAGS_0(name, flags, ret)                            \
+         GEN_ADAPTER_0(name, ret) \
 static inline void glue(gen_helper_, name)(TCGContext *tcg_ctx, dh_retvar_decl0(ret))        \
 {                                                                       \
-  tcg_gen_callN(tcg_ctx, HELPER(name), dh_retvar(ret), 0, NULL);                 \
+  tcg_gen_callN(tcg_ctx, glue(adapter_helper_, name), dh_retvar(ret), 0, NULL);                 \
 }
 
-#define DEF_HELPER_FLAGS_1(name, flags, ret, t1)                        \
+
+         #define DEF_HELPER_FLAGS_1(name, flags, ret, t1)                        \
+         GEN_ADAPTER_1(name, ret, t1) \
 static inline void glue(gen_helper_, name)(TCGContext *tcg_ctx, dh_retvar_decl(ret)          \
     dh_arg_decl(t1, 1))                                                 \
 {                                                                       \
   TCGTemp *args[1] = { dh_arg(t1, 1) };                                 \
-  tcg_gen_callN(tcg_ctx, HELPER(name), dh_retvar(ret), 1, args);                 \
+  tcg_gen_callN(tcg_ctx, glue(adapter_helper_, name), dh_retvar(ret), 1, args);                 \
 }
 
-#define DEF_HELPER_FLAGS_2(name, flags, ret, t1, t2)                    \
+
+         #define DEF_HELPER_FLAGS_2(name, flags, ret, t1, t2)                    \
+         GEN_ADAPTER_2(name, ret, t1, t2) \
 static inline void glue(gen_helper_, name)(TCGContext *tcg_ctx, dh_retvar_decl(ret)          \
     dh_arg_decl(t1, 1), dh_arg_decl(t2, 2))                             \
 {                                                                       \
   TCGTemp *args[2] = { dh_arg(t1, 1), dh_arg(t2, 2) };                  \
-  tcg_gen_callN(tcg_ctx, HELPER(name), dh_retvar(ret), 2, args);                 \
+  tcg_gen_callN(tcg_ctx, glue(adapter_helper_, name), dh_retvar(ret), 2, args);                 \
 }
 
-#define DEF_HELPER_FLAGS_3(name, flags, ret, t1, t2, t3)                \
+
+         #define DEF_HELPER_FLAGS_3(name, flags, ret, t1, t2, t3)                \
+         GEN_ADAPTER_3(name, ret, t1, t2, t3) \
 static inline void glue(gen_helper_, name)(TCGContext *tcg_ctx, dh_retvar_decl(ret)          \
     dh_arg_decl(t1, 1), dh_arg_decl(t2, 2), dh_arg_decl(t3, 3))         \
 {                                                                       \
   TCGTemp *args[3] = { dh_arg(t1, 1), dh_arg(t2, 2), dh_arg(t3, 3) };   \
-  tcg_gen_callN(tcg_ctx, HELPER(name), dh_retvar(ret), 3, args);                 \
+  tcg_gen_callN(tcg_ctx, glue(adapter_helper_, name), dh_retvar(ret), 3, args);                 \
 }
 
-#define DEF_HELPER_FLAGS_4(name, flags, ret, t1, t2, t3, t4)            \
+
+         #define DEF_HELPER_FLAGS_4(name, flags, ret, t1, t2, t3, t4) \
+         GEN_ADAPTER_4(name, ret, t1, t2, t3, t4) \
 static inline void glue(gen_helper_, name)(TCGContext *tcg_ctx, dh_retvar_decl(ret)          \
     dh_arg_decl(t1, 1), dh_arg_decl(t2, 2),                             \
     dh_arg_decl(t3, 3), dh_arg_decl(t4, 4))                             \
 {                                                                       \
   TCGTemp *args[4] = { dh_arg(t1, 1), dh_arg(t2, 2),                    \
                      dh_arg(t3, 3), dh_arg(t4, 4) };                    \
-  tcg_gen_callN(tcg_ctx, HELPER(name), dh_retvar(ret), 4, args);                 \
+  tcg_gen_callN(tcg_ctx, glue(adapter_helper_, name), dh_retvar(ret), 4, args);                 \
 }
 
-#define DEF_HELPER_FLAGS_5(name, flags, ret, t1, t2, t3, t4, t5)        \
+
+         #define DEF_HELPER_FLAGS_5(name, flags, ret, t1, t2, t3, t4, t5)        \
+         GEN_ADAPTER_5(name, ret, t1, t2, t3, t4, t5) \
 static inline void glue(gen_helper_, name)(TCGContext *tcg_ctx, dh_retvar_decl(ret)          \
     dh_arg_decl(t1, 1),  dh_arg_decl(t2, 2), dh_arg_decl(t3, 3),        \
     dh_arg_decl(t4, 4), dh_arg_decl(t5, 5))                             \
 {                                                                       \
   TCGTemp *args[5] = { dh_arg(t1, 1), dh_arg(t2, 2), dh_arg(t3, 3),     \
                      dh_arg(t4, 4), dh_arg(t5, 5) };                    \
-  tcg_gen_callN(tcg_ctx, HELPER(name), dh_retvar(ret), 5, args);                 \
+  tcg_gen_callN(tcg_ctx, glue(adapter_helper_, name), dh_retvar(ret), 5, args);                 \
 }
 
 #define DEF_HELPER_FLAGS_6(name, flags, ret, t1, t2, t3, t4, t5, t6)    \
@@ -63,7 +75,7 @@ static inline void glue(gen_helper_, name)(TCGContext *tcg_ctx, dh_retvar_decl(r
 {                                                                       \
   TCGTemp *args[6] = { dh_arg(t1, 1), dh_arg(t2, 2), dh_arg(t3, 3),     \
                      dh_arg(t4, 4), dh_arg(t5, 5), dh_arg(t6, 6) };     \
-  tcg_gen_callN(tcg_ctx, HELPER(name), dh_retvar(ret), 6, args);                 \
+  tcg_gen_callN(tcg_ctx, glue(adapter_helper_, name), dh_retvar(ret), 6, args);                 \
 }
 
 #define DEF_HELPER_FLAGS_7(name, flags, ret, t1, t2, t3, t4, t5, t6, t7)\
@@ -75,7 +87,7 @@ static inline void glue(gen_helper_, name)(TCGContext *tcg_ctx, dh_retvar_decl(r
   TCGTemp *args[7] = { dh_arg(t1, 1), dh_arg(t2, 2), dh_arg(t3, 3),     \
                      dh_arg(t4, 4), dh_arg(t5, 5), dh_arg(t6, 6),       \
                      dh_arg(t7, 7) };                                   \
-  tcg_gen_callN(tcg_ctx, HELPER(name), dh_retvar(ret), 7, args);                 \
+  tcg_gen_callN(tcg_ctx, glue(adapter_helper_, name), dh_retvar(ret), 7, args);                 \
 }
 
 #include "helper.h"
diff --git a/qemu/include/exec/helper-proto.h b/qemu/include/exec/helper-proto.h
index 217d670a..96386541 100644
--- a/qemu/include/exec/helper-proto.h
+++ b/qemu/include/exec/helper-proto.h
@@ -6,23 +6,35 @@
 
 #include "exec/helper-head.h"
 
-#define DEF_HELPER_FLAGS_0(name, flags, ret) \
+
+         #define DEF_HELPER_FLAGS_0(name, flags, ret) \
+         GEN_ADAPTER_DECLARE(name) \
 dh_ctype(ret) HELPER(name) (void);
 
-#define DEF_HELPER_FLAGS_1(name, flags, ret, t1) \
+
+         #define DEF_HELPER_FLAGS_1(name, flags, ret, t1) \
+         GEN_ADAPTER_DECLARE(name) \
 dh_ctype(ret) HELPER(name) (dh_ctype(t1));
 
-#define DEF_HELPER_FLAGS_2(name, flags, ret, t1, t2) \
+
+         #define DEF_HELPER_FLAGS_2(name, flags, ret, t1, t2) \
+         GEN_ADAPTER_DECLARE(name) \
 dh_ctype(ret) HELPER(name) (dh_ctype(t1), dh_ctype(t2));
 
-#define DEF_HELPER_FLAGS_3(name, flags, ret, t1, t2, t3) \
+
+         #define DEF_HELPER_FLAGS_3(name, flags, ret, t1, t2, t3) \
+         GEN_ADAPTER_DECLARE(name) \
 dh_ctype(ret) HELPER(name) (dh_ctype(t1), dh_ctype(t2), dh_ctype(t3));
 
-#define DEF_HELPER_FLAGS_4(name, flags, ret, t1, t2, t3, t4) \
+
+         #define DEF_HELPER_FLAGS_4(name, flags, ret, t1, t2, t3, t4) \
+         GEN_ADAPTER_DECLARE(name) \
 dh_ctype(ret) HELPER(name) (dh_ctype(t1), dh_ctype(t2), dh_ctype(t3), \
                                    dh_ctype(t4));
 
-#define DEF_HELPER_FLAGS_5(name, flags, ret, t1, t2, t3, t4, t5) \
+
+         #define DEF_HELPER_FLAGS_5(name, flags, ret, t1, t2, t3, t4, t5) \
+         GEN_ADAPTER_DECLARE(name) \
 dh_ctype(ret) HELPER(name) (dh_ctype(t1), dh_ctype(t2), dh_ctype(t3), \
                             dh_ctype(t4), dh_ctype(t5));
 
diff --git a/qemu/include/exec/helper-tcg.h b/qemu/include/exec/helper-tcg.h
index 42d145b8..e54a8db3 100644
--- a/qemu/include/exec/helper-tcg.h
+++ b/qemu/include/exec/helper-tcg.h
@@ -11,49 +11,49 @@
 #define str(s) #s
 
 #define DEF_HELPER_FLAGS_0(NAME, FLAGS, ret) \
-  { .func = HELPER(NAME), .name = str(NAME), \
+  { .func = glue(adapter_helper_, NAME), .name = str(NAME), \
     .flags = FLAGS | dh_callflag(ret), \
     .sizemask = dh_sizemask(ret, 0) },
 
 #define DEF_HELPER_FLAGS_1(NAME, FLAGS, ret, t1) \
-  { .func = HELPER(NAME), .name = str(NAME), \
+  { .func = glue(adapter_helper_, NAME), .name = str(NAME), \
     .flags = FLAGS | dh_callflag(ret), \
     .sizemask = dh_sizemask(ret, 0) | dh_sizemask(t1, 1) },
 
 #define DEF_HELPER_FLAGS_2(NAME, FLAGS, ret, t1, t2) \
-  { .func = HELPER(NAME), .name = str(NAME), \
+  { .func = glue(adapter_helper_, NAME), .name = str(NAME), \
     .flags = FLAGS | dh_callflag(ret), \
     .sizemask = dh_sizemask(ret, 0) | dh_sizemask(t1, 1) \
     | dh_sizemask(t2, 2) },
 
 #define DEF_HELPER_FLAGS_3(NAME, FLAGS, ret, t1, t2, t3) \
-  { .func = HELPER(NAME), .name = str(NAME), \
+  { .func = glue(adapter_helper_, NAME), .name = str(NAME), \
     .flags = FLAGS | dh_callflag(ret), \
     .sizemask = dh_sizemask(ret, 0) | dh_sizemask(t1, 1) \
     | dh_sizemask(t2, 2) | dh_sizemask(t3, 3) },
 
 #define DEF_HELPER_FLAGS_4(NAME, FLAGS, ret, t1, t2, t3, t4) \
-  { .func = HELPER(NAME), .name = str(NAME), \
+  { .func = glue(adapter_helper_, NAME), .name = str(NAME), \
     .flags = FLAGS | dh_callflag(ret), \
     .sizemask = dh_sizemask(ret, 0) | dh_sizemask(t1, 1) \
     | dh_sizemask(t2, 2) | dh_sizemask(t3, 3) | dh_sizemask(t4, 4) },
 
 #define DEF_HELPER_FLAGS_5(NAME, FLAGS, ret, t1, t2, t3, t4, t5) \
-  { .func = HELPER(NAME), .name = str(NAME), \
+  { .func = glue(adapter_helper_, NAME), .name = str(NAME), \
     .flags = FLAGS | dh_callflag(ret), \
     .sizemask = dh_sizemask(ret, 0) | dh_sizemask(t1, 1) \
     | dh_sizemask(t2, 2) | dh_sizemask(t3, 3) | dh_sizemask(t4, 4) \
     | dh_sizemask(t5, 5) },
 
 #define DEF_HELPER_FLAGS_6(NAME, FLAGS, ret, t1, t2, t3, t4, t5, t6) \
-  { .func = HELPER(NAME), .name = str(NAME), \
+  { .func = glue(adapter_helper_, NAME), .name = str(NAME), \
     .flags = FLAGS | dh_callflag(ret), \
     .sizemask = dh_sizemask(ret, 0) | dh_sizemask(t1, 1) \
     | dh_sizemask(t2, 2) | dh_sizemask(t3, 3) | dh_sizemask(t4, 4) \
     | dh_sizemask(t5, 5) | dh_sizemask(t6, 6) },
 
 #define DEF_HELPER_FLAGS_7(NAME, FLAGS, ret, t1, t2, t3, t4, t5, t6, t7) \
-  { .func = HELPER(NAME), .name = str(NAME), .flags = FLAGS, \
+  { .func = glue(adapter_helper_, NAME), .name = str(NAME), .flags = FLAGS, \
     .sizemask = dh_sizemask(ret, 0) | dh_sizemask(t1, 1) \
     | dh_sizemask(t2, 2) | dh_sizemask(t3, 3) | dh_sizemask(t4, 4) \
     | dh_sizemask(t5, 5) | dh_sizemask(t6, 6) | dh_sizemask(t7, 7) },
diff --git a/qemu/include/qemu-common.h b/qemu/include/qemu-common.h
index 30d897fd..b921d6d6 100644
--- a/qemu/include/qemu-common.h
+++ b/qemu/include/qemu-common.h
@@ -82,3 +82,49 @@ void page_size_init(struct uc_struct *uc);
 CPUState *qemu_get_cpu(struct uc_struct *uc, int index);
 
 #endif
+
+#define UNALIGNED_READ16_LE(addr) ( \
+    ((uint16_t)(*((uint8_t*)(addr) + 0)) <<  0) |  \
+    ((uint16_t)(*((uint8_t*)(addr) + 1)) <<  8)    \
+)
+
+#define UNALIGNED_READ32_LE(addr) ( \
+    ((uint32_t)(*((uint8_t*)(addr) + 0)) <<  0) |  \
+    ((uint32_t)(*((uint8_t*)(addr) + 1)) <<  8) |  \
+    ((uint32_t)(*((uint8_t*)(addr) + 2)) << 16) |  \
+    ((uint32_t)(*((uint8_t*)(addr) + 3)) << 24)    \
+)
+
+#define UNALIGNED_READ64_LE(addr) ( \
+    ((uint64_t)(*((uint8_t*)(addr) + 0)) <<  0) |  \
+    ((uint64_t)(*((uint8_t*)(addr) + 1)) <<  8) |  \
+    ((uint64_t)(*((uint8_t*)(addr) + 2)) << 16) |  \
+    ((uint64_t)(*((uint8_t*)(addr) + 3)) << 24) |  \
+    ((uint64_t)(*((uint8_t*)(addr) + 4)) << 32) |  \
+    ((uint64_t)(*((uint8_t*)(addr) + 5)) << 40) |  \
+    ((uint64_t)(*((uint8_t*)(addr) + 6)) << 48) |  \
+    ((uint64_t)(*((uint8_t*)(addr) + 7)) << 56)    \
+)
+
+#define UNALIGNED_WRITE16_LE(addr, value) { \
+    *((uint8_t*)(addr) + 0) = ((value) >>  0) & 0xFF; \
+    *((uint8_t*)(addr) + 1) = ((value) >>  8) & 0xFF; \
+}
+
+#define UNALIGNED_WRITE32_LE(addr, value) { \
+    *((uint8_t*)(addr) + 0) = ((value) >>  0) & 0xFF; \
+    *((uint8_t*)(addr) + 1) = ((value) >>  8) & 0xFF; \
+    *((uint8_t*)(addr) + 2) = ((value) >> 16) & 0xFF; \
+    *((uint8_t*)(addr) + 3) = ((value) >> 24) & 0xFF; \
+}
+
+#define UNALIGNED_WRITE64_LE(addr, value) { \
+    *((uint8_t*)(addr) + 0) = ((value) >>  0) & 0xFF; \
+    *((uint8_t*)(addr) + 1) = ((value) >>  8) & 0xFF; \
+    *((uint8_t*)(addr) + 2) = ((value) >> 16) & 0xFF; \
+    *((uint8_t*)(addr) + 3) = ((value) >> 24) & 0xFF; \
+    *((uint8_t*)(addr) + 4) = ((value) >> 32) & 0xFF; \
+    *((uint8_t*)(addr) + 5) = ((value) >> 40) & 0xFF; \
+    *((uint8_t*)(addr) + 6) = ((value) >> 48) & 0xFF; \
+    *((uint8_t*)(addr) + 7) = ((value) >> 56) & 0xFF; \
+}
diff --git a/qemu/include/qemu/int128.h b/qemu/include/qemu/int128.h
index d1d1ad44..7b5f290f 100644
--- a/qemu/include/qemu/int128.h
+++ b/qemu/include/qemu/int128.h
@@ -147,7 +147,7 @@ static inline Int128 bswap128(Int128 a)
 
 typedef struct Int128 Int128;
 #if !(defined(_MSC_VER) && defined(__clang__))
-typedef Int128 __int128_t;
+// typedef Int128 __int128_t; // Disabled for Emscripten compatibility
 #endif
 
 struct Int128 {
diff --git a/qemu/target/arm/translate.c b/qemu/target/arm/translate.c
index 744d8ff7..c57919cc 100644
--- a/qemu/target/arm/translate.c
+++ b/qemu/target/arm/translate.c
@@ -1,3 +1,4 @@
+#define GEN_ADAPTER_DEFINE
 /*
  *  ARM translation
  *
diff --git a/qemu/target/i386/translate.c b/qemu/target/i386/translate.c
index 8278774a..84752997 100644
--- a/qemu/target/i386/translate.c
+++ b/qemu/target/i386/translate.c
@@ -1,3 +1,4 @@
+#define GEN_ADAPTER_DEFINE
 /*
  *  i386 translation
  *
diff --git a/qemu/target/m68k/translate.c b/qemu/target/m68k/translate.c
index 5b9a74ce..99f6f4b3 100644
--- a/qemu/target/m68k/translate.c
+++ b/qemu/target/m68k/translate.c
@@ -1,3 +1,4 @@
+#define GEN_ADAPTER_DEFINE
 /*
  *  m68k translation
  *
diff --git a/qemu/target/mips/translate.c b/qemu/target/mips/translate.c
index 3fab57b2..02a0d314 100644
--- a/qemu/target/mips/translate.c
+++ b/qemu/target/mips/translate.c
@@ -1,3 +1,4 @@
+#define GEN_ADAPTER_DEFINE
 /*
  *  MIPS emulation for QEMU - main translation routines
  *
diff --git a/qemu/target/ppc/translate.c b/qemu/target/ppc/translate.c
index 0b3e5c8c..375b5497 100644
--- a/qemu/target/ppc/translate.c
+++ b/qemu/target/ppc/translate.c
@@ -1,3 +1,4 @@
+#define GEN_ADAPTER_DEFINE
 /*
  *  PowerPC emulation for qemu: main translation routines.
  *
diff --git a/qemu/target/riscv/translate.c b/qemu/target/riscv/translate.c
index 792bc12f..8c3b86d7 100644
--- a/qemu/target/riscv/translate.c
+++ b/qemu/target/riscv/translate.c
@@ -1,3 +1,4 @@
+#define GEN_ADAPTER_DEFINE
 /*
  * RISC-V emulation for qemu: main translation routines.
  *
diff --git a/qemu/target/s390x/translate.c b/qemu/target/s390x/translate.c
index e41a3b73..c319977d 100644
--- a/qemu/target/s390x/translate.c
+++ b/qemu/target/s390x/translate.c
@@ -1,3 +1,4 @@
+#define GEN_ADAPTER_DEFINE
 /*
  *  S/390 translation
  *
diff --git a/qemu/target/sparc/translate.c b/qemu/target/sparc/translate.c
index 95b24ab6..558abcee 100644
--- a/qemu/target/sparc/translate.c
+++ b/qemu/target/sparc/translate.c
@@ -1,3 +1,4 @@
+#define GEN_ADAPTER_DEFINE
 /*
    SPARC translation
 
diff --git a/qemu/target/tricore/translate.c b/qemu/target/tricore/translate.c
index 75188b8b..366ae677 100644
--- a/qemu/target/tricore/translate.c
+++ b/qemu/target/tricore/translate.c
@@ -1,3 +1,4 @@
+#define GEN_ADAPTER_DEFINE
 /*
  *  TriCore emulation for qemu: main translation routines.
  *
diff --git a/qemu/tcg/tcg.c b/qemu/tcg/tcg.c
index bc62051f..6cb3dc8f 100644
--- a/qemu/tcg/tcg.c
+++ b/qemu/tcg/tcg.c
@@ -1444,7 +1444,19 @@ void tcg_gen_callN(TCGContext *tcg_ctx, void *func, TCGTemp *ret, int nargs, TCG
 
     info = g_hash_table_lookup(tcg_ctx->helper_table, (gpointer)func);
     flags = info->flags;
-    sizemask = info->sizemask;
+    
+         sizemask = info->sizemask;
+
+         for (i = 0; i < nargs; i++) {
+             int is_64bit = sizemask & (1 << (i+1)*2);
+             if (!is_64bit) {
+                 TCGv_i64 ext_arg = tcg_temp_new_i64(s);
+                 TCGv_i64 orig = MAKE_TCGV_I64(args[i]);
+                 tcg_gen_ext32u_i64(s, ext_arg, orig);
+                 args[i] = GET_TCGV_I64(ext_arg);
+             }
+         }
+        
 
 #if defined(__sparc__) && !defined(__arch64__)
     /* We have 64-bit values in one register, but need to pass as two
@@ -1458,7 +1470,7 @@ void tcg_gen_callN(TCGContext *tcg_ctx, void *func, TCGTemp *ret, int nargs, TCG
     reth = NULL;
     if (sizemask != 0) {
         for (i = real_args = 0; i < nargs; ++i) {
-            int is_64bit = sizemask & (1 << (i+1)*2);
+            int is_64bit = 1;//sizemask & (1 << (i+1)*2);
             if (is_64bit) {
                 TCGv_i64 orig = temp_tcgv_i64(args[i]);
                 TCGv_i32 h = tcg_temp_new_i32();
@@ -1476,7 +1488,7 @@ void tcg_gen_callN(TCGContext *tcg_ctx, void *func, TCGTemp *ret, int nargs, TCG
     }
 #elif defined(TCG_TARGET_EXTEND_ARGS) && TCG_TARGET_REG_BITS == 64
     for (i = 0; i < nargs; ++i) {
-        int is_64bit = sizemask & (1 << (i+1)*2);
+        int is_64bit = 1;//sizemask & (1 << (i+1)*2);
         int is_signed = sizemask & (2 << (i+1)*2);
         if (!is_64bit) {
             TCGv_i64 temp = tcg_temp_new_i64(tcg_ctx);
@@ -1531,7 +1543,7 @@ void tcg_gen_callN(TCGContext *tcg_ctx, void *func, TCGTemp *ret, int nargs, TCG
 
     real_args = 0;
     for (i = 0; i < nargs; i++) {
-        int is_64bit = sizemask & (1 << (i+1)*2);
+        int is_64bit = 1;//sizemask & (1 << (i+1)*2);
         if (TCG_TARGET_REG_BITS < 64 && is_64bit) {
 #ifdef TCG_TARGET_CALL_ALIGN_ARGS
             /* some targets want aligned 64 bit args */
@@ -1575,7 +1587,7 @@ void tcg_gen_callN(TCGContext *tcg_ctx, void *func, TCGTemp *ret, int nargs, TCG
 #if defined(__sparc__) && !defined(__arch64__)
     /* Free all of the parts we allocated above.  */
     for (i = real_args = 0; i < orig_nargs; ++i) {
-        int is_64bit = orig_sizemask & (1 << (i+1)*2);
+        int is_64bit = 1;//orig_sizemask & (1 << (i+1)*2);
         if (is_64bit) {
             tcg_temp_free_internal(args[real_args++]);
             tcg_temp_free_internal(args[real_args++]);
@@ -1593,7 +1605,7 @@ void tcg_gen_callN(TCGContext *tcg_ctx, void *func, TCGTemp *ret, int nargs, TCG
     }
 #elif defined(TCG_TARGET_EXTEND_ARGS) && TCG_TARGET_REG_BITS == 64
     for (i = 0; i < nargs; ++i) {
-        int is_64bit = sizemask & (1 << (i+1)*2);
+        int is_64bit = 1;//sizemask & (1 << (i+1)*2);
         if (!is_64bit) {
             tcg_temp_free_internal(tcg_ctx, args[i]);
         }
diff --git a/qemu/util/osdep.c b/qemu/util/osdep.c
index 148e37a5..b3468818 100644
--- a/qemu/util/osdep.c
+++ b/qemu/util/osdep.c
@@ -1,3 +1,18 @@
+#ifdef __EMSCRIPTEN__
+// Stub mprotect for Emscripten
+#include <stddef.h>
+#define PROT_NONE 0
+#define PROT_READ 1
+#define PROT_WRITE 2
+#define PROT_EXEC 4
+static inline int mprotect(void *addr, size_t len, int prot) {
+    // Emscripten doesn't support mprotect, just return success
+    return 0;
+}
+#else
+#include <sys/mman.h>
+#endif
+
 /*
  * QEMU low level functions
  *
diff --git a/qemu/util/oslib-posix.c b/qemu/util/oslib-posix.c
index 615e477e..25e7de81 100644
--- a/qemu/util/oslib-posix.c
+++ b/qemu/util/oslib-posix.c
@@ -1,3 +1,33 @@
+#ifdef __EMSCRIPTEN__
+// Stub mmap functions for Emscripten
+#include <stddef.h>
+#include <stdlib.h>
+#include <sys/types.h>
+#define MAP_FAILED ((void *) -1)
+#define MAP_PRIVATE 0x02
+#define MAP_ANONYMOUS 0x20
+#define MAP_ANON MAP_ANONYMOUS
+#define MAP_SHARED 0x01
+#define MAP_FIXED 0x10
+#define PROT_NONE 0
+#define PROT_READ 1
+#define PROT_WRITE 2
+#define PROT_EXEC 4
+static inline void *mmap(void *addr, size_t length, int prot, int flags, int fd, off_t offset) {
+    // Emscripten doesn't support mmap, use malloc as fallback
+    if (addr && (flags & MAP_FIXED)) {
+        // If fixed address requested, we can't honor it, return failure
+        return MAP_FAILED;
+    }
+    void *ptr = malloc(length);
+    return ptr ? ptr : MAP_FAILED;
+}
+static inline int munmap(void *addr, size_t length) {
+    free(addr);
+    return 0;
+}
+#endif
+
 /*
  * os-posix-lib.c
  *
diff --git a/uc.c b/uc.c
index 75b89a01..413cbfc9 100644
--- a/uc.c
+++ b/uc.c
@@ -1906,7 +1906,7 @@ uc_err uc_mem_unmap(struct uc_struct *uc, uint64_t address, uint64_t size)
 
 UNICORN_EXPORT
 uc_err uc_hook_add(uc_engine *uc, uc_hook *hh, int type, void *callback,
-                   void *user_data, uint64_t begin, uint64_t end, ...)
+                   void *user_data, uint64_t begin, uint64_t end, uint32_t extra)
 {
     int ret = UC_ERR_OK;
     int i = 0;
@@ -1932,11 +1932,11 @@ uc_err uc_hook_add(uc_engine *uc, uc_hook *hh, int type, void *callback,
 
     // UC_HOOK_INSN has an extra argument for instruction ID
     if (type & UC_HOOK_INSN) {
-        va_list valist;
+        //va_list valist;
 
-        va_start(valist, end);
-        hook->insn = va_arg(valist, int);
-        va_end(valist);
+        //va_start(valist, end);
+        hook->insn = extra;
+        //va_end(valist);
 
         if (uc->insn_hook_validate) {
             if (!uc->insn_hook_validate(hook->insn)) {
@@ -1966,12 +1966,12 @@ uc_err uc_hook_add(uc_engine *uc, uc_hook *hh, int type, void *callback,
     }
 
     if (type & UC_HOOK_TCG_OPCODE) {
-        va_list valist;
+        //va_list valist;
 
-        va_start(valist, end);
+        //va_start(valist, end);
         hook->op = va_arg(valist, int);
         hook->op_flags = va_arg(valist, int);
-        va_end(valist);
+        //va_end(valist);
 
         if (uc->opcode_hook_invalidate) {
             if (!uc->opcode_hook_invalidate(hook->op, hook->op_flags)) {
